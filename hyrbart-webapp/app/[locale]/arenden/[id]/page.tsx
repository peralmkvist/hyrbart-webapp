import Link from 'next/link';import{redirect}from'next/navigation';import{createClient}from'@/lib/supabase/server';import CaseDetail from'@/components/CaseDetail';
export default async function CasePage({params}:{params:Promise<{locale:string;id:string}>}){const{locale,id}=await params;const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect(`/topsecret/${locale}/logga-in?next=${encodeURIComponent(`/${locale}/arenden/${id}`)}`);return <main className="casePage"><header className="casePageTop"><Link href={`/${locale}/kalender`}>‹</Link><h1>Ärende</h1></header><CaseDetail caseId={id}/></main>}
