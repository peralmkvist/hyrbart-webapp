export const BOOKING_FEE = 9;
export type RentalPriceTier={days:number;price:number};export type RentalDiscountRules={multiDayPercent?:number;weeklyPercent?:number;repeatCustomerPercent?:number};export type RentalPricing={days:number;dailyPrice:number;regularRental:number;rentalCost:number;discount:number;discountPercent:number;discountType?:'multiDay'|'weekly'|'repeatCustomer';hasWeeklyDiscount:boolean;bookingFee:number;total:number};
export function dailyNumber(price:string):number|null{const match=price.match(/([0-9]+(?:[.,][0-9]+)?)/);return match?Number(match[1].replace(',','.')):null}
export function rentalDays(from?:string,to?:string):number|null{if(!from)return null;const start=new Date(`${from}T12:00:00`),end=new Date(`${to||from}T12:00:00`);return Math.max(1,Math.round((end.getTime()-start.getTime())/86400000)+1)}
export function bestRentalCost(days:number,daily:number,prices?:RentalPriceTier[]):number{const tiers=[{days:1,price:daily},...(prices??[]).filter(t=>t.days>1&&t.price>0)],dp=Array(days+1).fill(Number.POSITIVE_INFINITY);dp[0]=0;for(let currentDay=1;currentDay<=days;currentDay+=1)for(const tier of tiers)if(tier.days<=currentDay)dp[currentDay]=Math.min(dp[currentDay],dp[currentDay-tier.days]+tier.price);return Number.isFinite(dp[days])?Math.round(dp[days]):Math.round(days*daily)}
function clampPercent(value?:number):number{if(!Number.isFinite(value))return 0;return Math.max(0,Math.min(90,Number(value)))}
export function achievableDailyPriceRange(priceLabel:string,prices?:RentalPriceTier[],discountRules?:RentalDiscountRules){const daily=dailyNumber(priceLabel);if(!daily)return null;const candidates=[daily];for(const tier of prices??[])if(tier.days>0&&tier.price>0)candidates.push(tier.price/tier.days);const multi=clampPercent(discountRules?.multiDayPercent),weekly=clampPercent(discountRules?.weeklyPercent);if(multi>0)candidates.push(daily*(1-multi/100));if(weekly>0)candidates.push(daily*(1-weekly/100));/* Repeat-customer discounts are deliberately excluded: not every user can achieve them. */const min=Math.round(Math.min(...candidates)),max=Math.round(daily);return{min,max,label:min<max?`${min}–${max} kr/dygn`:`${max} kr/dygn`}}
export function calculateRentalPricing(priceLabel:string,from?:string,to?:string,prices?:RentalPriceTier[],discountRules?:RentalDiscountRules,repeatCustomer=false):RentalPricing|null{const days=rentalDays(from,to),dailyPrice=dailyNumber(priceLabel);if(!days||!dailyPrice)return null;const regularRental=Math.round(days*dailyPrice),multiDayPercent=days>=2&&days<=6?clampPercent(discountRules?.multiDayPercent):0,weeklyPercent=days>=7?clampPercent(discountRules?.weeklyPercent):0,repeatPercent=repeatCustomer?clampPercent(discountRules?.repeatCustomerPercent):0,configuredPercent=Math.max(multiDayPercent,weeklyPercent,repeatPercent);let rentalCost:number,discountType:RentalPricing['discountType'];if(configuredPercent>0){rentalCost=Math.round(regularRental*(1-configuredPercent/100));discountType=configuredPercent===repeatPercent&&repeatPercent>Math.max(multiDayPercent,weeklyPercent)?'repeatCustomer':weeklyPercent>=multiDayPercent&&weeklyPercent>0?'weekly':'multiDay'}else{rentalCost=bestRentalCost(days,dailyPrice,prices);if(rentalCost<regularRental)discountType=days>=7?'weekly':'multiDay'}const discount=Math.max(0,regularRental-rentalCost),discountPercent=discount>0?Math.round(discount/regularRental*100):0;return{days,dailyPrice,regularRental,rentalCost,discount,discountPercent,discountType,hasWeeklyDiscount:discountType==='weekly',bookingFee:BOOKING_FEE,total:rentalCost+BOOKING_FEE}}
