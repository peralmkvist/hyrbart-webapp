'use client';import{useEffect,useMemo,useState}from'react';import{useRouter}from'next/navigation';
const labels:any={open:'Öppet',under_review:'Under granskning',resolved:'Löst',rejected:'Avslaget'};const typeLabels:any={problem:'Problem',damage:'Skada',dispute:'Tvist',cancellation:'Avbokningstvist'};
export default function AdminCaseDetail({caseId,locale}:{caseId:string;locale:string}){const r=useRouter(),[d,setD]=useState<any>(null),[status,setStatus]=useState('under_review'),[note,setNote]=useState(''),[decision,setDecision]=useState(''),[amount,setAmount]=useState(''),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function load(){const x=await fetch(`/api/admin/cases/${caseId}`,{cache:'no-store'}),j=await x.json();setD(j);if(j.case)setStatus(j.case.status)}useEffect(()=>{load()},[caseId]);const caseEvents=useMemo(()=>!d?[]:(d.events||[]).filter((e:any)=>e.metadata?.case_id===caseId||e.event_type.startsWith('case_')),[d,caseId]);if(!d)return <div className="adminEmpty">Laddar…</div>;if(d.error)return <div className="adminEmpty">Ärendet kunde inte öppnas.</div>;const profiles=new Map((d.profiles||[]).map((x:any)=>[x.id,x]));const owner:any=profiles.get(d.booking?.owner_id),renter:any=profiles.get(d.booking?.renter_id);const pickup=(d.conditionPhotos||[]).filter((x:any)=>x.stage==='pickup'),returned=(d.conditionPhotos||[]).filter((x:any)=>x.stage==='return');async function save(){setBusy(true);setMsg('');const x=await fetch(`/api/admin/cases/${caseId}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status,note,decision,amount})});const j=await x.json().catch(()=>({}));if(x.ok){setMsg('Ärendet är uppdaterat.');setNote('');setDecision('');setAmount('');await load();r.refresh()}else setMsg(j.error==='DECISION_REQUIRED'?'Skriv en motivering på minst 10 tecken när ärendet avslutas.':'Kunde inte uppdatera ärendet.');setBusy(false)}return <div className="adminDetail"><section className="adminCaseHero"><span>{typeLabels[d.case.case_type]||d.case.case_type}</span><h1>{d.case.reason}</h1><p>{d.case.description||'Ingen beskrivning.'}</p><b>{labels[d.case.status]||d.case.status}</b></section><section className="adminPartyGrid"><div><span>HYRARE</span><strong>{renter?.display_name||'–'}</strong><small>{renter?.email||''}</small></div><div><span>UTHYRARE</span><strong>{owner?.display_name||'–'}</strong><small>{owner?.email||''}</small></div></section><section className="adminPanel"><div className="adminPanelTitle"><h2>Bokning</h2><a href={`/${locale}/bokningar/${d.booking?.id}`} target="_blank" rel="noreferrer">Öppna bokning ↗</a></div><div className="adminFacts"><span>Status <b>{d.booking?.status}</b></span><span>Period <b>{d.booking?.start_date} – {d.booking?.end_date}</b></span><span>Totalsumma <b>{Number(d.booking?.total_price||0).toLocaleString('sv-SE')} kr</b></span><span>Ersättningskrav <b>{d.case.amount_claimed?`${Number(d.case.amount_claimed).toLocaleString('sv-SE')} kr`:'–'}</b></span></div></section><section className="adminPanel"><h2>Skickbilder från bokningen</h2><div className="adminEvidenceColumns"><EvidenceGroup title="Utlämning" items={pickup}/><EvidenceGroup title="Återlämning" items={returned}/></div></section><section className="adminPanel"><h2>Bilagor i ärendet</h2>{(d.evidence||[]).length?<div className="adminEvidenceGrid">{d.evidence.map((x:any)=><a key={x.id} className="adminEvidenceItem" href={x.url||'#'} target="_blank" rel="noreferrer"><div>{x.mime_type?.startsWith('image/')&&x.url?<img src={x.url} alt=""/>:<span className="adminPdf">PDF</span>}</div><strong>{x.file_name||'Bilaga'}</strong><small>{x.uploader_name||'Användare'} · {new Date(x.created_at).toLocaleString('sv-SE')}</small></a>)}</div>:<p className="adminMuted">Inga extra bilagor har laddats upp.</p>}</section><section className="adminPanel"><h2>Redogörelser & historik</h2><div className="adminTimeline">{caseEvents.length?caseEvents.map((e:any)=><article key={e.id}><b>{eventLabel(e.event_type)}</b><small>{new Date(e.created_at).toLocaleString('sv-SE')}</small>{e.metadata?.text?<p>{e.metadata.text}</p>:null}{e.metadata?.decision?<p><strong>Beslut:</strong> {e.metadata.decision}</p>:null}{e.metadata?.amount!=null?<p><strong>Belopp:</strong> {Number(e.metadata.amount).toLocaleString('sv-SE')} kr</p>:null}{e.metadata?.note?<p><strong>Intern notering:</strong> {e.metadata.note}</p>:null}</article>):<p className="adminMuted">Ingen ytterligare ärendehistorik ännu.</p>}</div></section><section className="adminPanel adminDecision"><h2>Handlägg ärendet</h2><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="open">Öppet</option><option value="under_review">Under granskning</option><option value="resolved">Löst</option><option value="rejected">Avslaget</option></select></label><label>Intern handläggarnotering<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Syns endast i adminhistoriken."/></label><label>Beslut / motivering<textarea value={decision} onChange={e=>setDecision(e.target.value)} placeholder={['resolved','rejected'].includes(status)?'Obligatoriskt när ärendet avslutas.':'Dokumentera beslutet och varför.'}/></label><label>Återbetalning / ersättning (kr)<input inputMode="numeric" value={amount} onChange={e=>setAmount(e.target.value.replace(/\D/g,''))} placeholder="0"/></label><button disabled={busy} onClick={save}>{busy?'Sparar…':'Spara beslut'}</button>{msg?<p className="adminMessage">{msg}</p>:null}</section></div>}
function EvidenceGroup({title,items}:{title:string;items:any[]}){return <div className="adminEvidenceGroup"><strong>{title}</strong>{items.length?<div className="adminConditionGrid">{items.map(x=>x.url?<a key={x.id} href={x.url} target="_blank" rel="noreferrer"><img src={x.url} alt=""/><small>{new Date(x.created_at).toLocaleString('sv-SE')}</small></a>:null)}</div>:<small>Ingen bild finns.</small>}</div>}
function eventLabel(type:string){const map:any={case_problem_opened:'Problem rapporterat',case_damage_opened:'Skadeärende öppnat',case_dispute_opened:'Tvist öppnad',case_statement_added:'Ny redogörelse',case_evidence_added:'Bilaga tillagd',case_admin_updated:'Support uppdaterade ärendet'};return map[type]||type.replaceAll('_',' ')}
